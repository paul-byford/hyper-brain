// Palette lifted straight from the app's dark theme (ui/styles.css :root) so the film and
// the product read as one identity: warm near-black ground, a single gold signal, a steel
// secondary for the "other" side of the graph.
export const C = {
  ground: "#100e09",
  panel: "#1a1710",
  panel2: "#241f15",
  hair: "#362f1d",
  ink: "#efe9da",
  muted: "#9a917c",
  faint: "#665f4c",
  signal: "#ce9b42",
  signalInk: "#dcb156",
  steel: "#7fa0b0",
} as const;
